apiVersion: v1
kind: Service
metadata:
  labels:
    app: hello-gitops-blue
  name: hello-gitops-blue
spec:
  ports:
    - name: http
      port: 8050
      targetPort: http
  selector:
    app: hello-gitops-blue
  type: ClusterIP
