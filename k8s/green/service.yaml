apiVersion: v1
kind: Service
metadata:
  labels:
    app: hello-gitops-green
  name: hello-gitops-green
spec:
  ports:
    - name: http
      port: 8050
      targetPort: http
  selector:
    app: hello-gitops-green
  type: ClusterIP
